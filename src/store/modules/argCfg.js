// import { OPEN_CAR_DETAIL } from 'store/mutation-types.js';

// state
const state = {
}

// getters
const getters = {
    // isOpenCarDetail(state) {
    //     return JSON.stringify(state.carDetail) !== '{}'
    // }
}

// actions
const actions = {
    // // 保存当前选中的车辆信息
    // saveCurCarDetail({ commit }, data) {
    //     commit({ type: OPEN_CAR_DETAIL, data })
    // }
}

// mutations
const mutations = {
    // // 保存当前选中的车辆信息
    // [OPEN_CAR_DETAIL](state, { data }) {
    //     state.carDetail = data || {};
    // }
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
